resources:
  jobs:
    bootstrap_uc:
      name: "Bootstrap UC (${bundle.target})"
      tasks:
        - task_key: bootstrap
          notebook_task:
            notebook_path: "${var.repo_root}/ADMIN/bootstrap_uc"
            base_parameters:
              catalog_name: ${var.catalog}
      run_as:
        user_name: "herman.lillejord@gmail.com"