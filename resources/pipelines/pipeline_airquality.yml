resources:
  pipelines:
    airquality_pipeline:
      name: "Air Quality Pipeline (${bundle.target})"
      development: true
      continuous: false
      channel: current
      photon: true
      serverless: true

      catalog: ${var.catalog}
      target: "${var.catalog}.air_quality"

      libraries:
        - notebook:
            path: "${var.repo_root}/MET/DATA INGEST/AirQualityPipeline"