resources:
  jobs:
    ingest_update_met:
      name: "Ingest MET Data (${bundle.target})"

      tasks:
        - task_key: ingest_update
          notebook_task:
            notebook_path: "${var.repo_root}/MET/DATA INGEST/Ingest Update MET"
            base_parameters:
              catalog: ${var.catalog}
              pipeline_id: ${var.pipeline_id}

      run_as:
        user_name: "herman.lillejord@gmail.com"