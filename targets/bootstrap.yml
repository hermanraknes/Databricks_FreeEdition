targets:
  bootstrap:
    mode: development
    default: false
    variables:
      repo_root: "/Workspace/Databricks_FreeEdition"
    resources:
      jobs:
        bootstrap_uc:
          name: "Create Catalogs"
        get_met_stations: !null
        ingest_update_met: !null
