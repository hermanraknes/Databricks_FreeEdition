targets:
  bootstrap:
    mode: development
    default: false
    variables:
      catalog: ${var.catalog}
      repo_root: "/Workspace/Databricks_FreeEdition"
    resources:
      jobs:
        bootstrap_uc:
          name: "Create Catalogs"
