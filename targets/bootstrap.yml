targets:
  bootstrap:
    mode: development
    default: false
    variables:
      repo_root: "/Workspace/Databricks_FreeEdition"
    resources:
      jobs:
        bootstrap_uc:
          name: "Create Catalogs"