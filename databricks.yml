bundle:
  name: met_bundle

include:
  - resources/*.yml

variables:
  catalog:
    description: "Unity Catalog to use (dev/prod)"
    default: main_uc_dev

targets:
  dev:
    default: true
    variables:
      catalog: main_uc_dev

  prod:
    variables:
      catalog: main_uc_prod
