name: DAB Validate + Deploy (Dev)

on:
  pull_request:
  push:
    branches: [ "main" ]
  workflow_dispatch:
    inputs:
      run_bootstrap:
        description: "Run bootstrap (Create UC/Schemas)"
        type: boolean
        default: false
      run_job:
        description: "Run ingest/pipeline after deploy?"
        type: boolean
        default: false

jobs:
  deploy:
    runs-on: ubuntu-latest

    env:
      DATABRICKS_HOST: ${{ secrets.DATABRICKS_HOST }}
      DATABRICKS_TOKEN: ${{ secrets.DATABRICKS_TOKEN }}

    steps:
      - uses: actions/checkout@v4

      - name: Install Databricks CLI
        uses: databricks/setup-cli@main

      # --- BOOTSTRAP ---
      - name: Deploy and run bootstrap
        if: github.event.inputs.run_bootstrap == 'true'
        run: |
          # Use -c to specify the bootstrap config
          databricks bundle deploy -c bootstrap.yml -t dev
          databricks bundle --config-file bootstrap.yml run bootstrap_uc -t dev
      
      # --- VALIDATION ---
      - name: Validate Dev Target
        run: databricks bundle validate -t dev
      
      # --- DEPLOYMENT ---
      - name: Deploy Dev Target
        if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'
        run: databricks bundle deploy -t dev --auto-approve

      # --- OPTIONAL RUN ---
      - name: Run main dev jobs and pipeline
        if: github.event.inputs.run_job == 'true'
        run: |
          databricks bundle run get_met_stations -t dev
          databricks bundle run ingest_update_met -t dev
          databricks bundle run airquality_pipeline -t dev